# Service agent instructions

This file governs the `service/` directory level. It uses the `AGENT.md`
migration alias (no `AGENTS.md` at this level) — the spec's own
`ln -s AGENTS.md AGENT.md` rename target.
