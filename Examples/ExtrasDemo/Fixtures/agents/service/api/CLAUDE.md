# API agent instructions

This file governs the `service/api/` directory level — the alias-only
directory: it holds neither `AGENTS.md` nor `AGENT.md`, only the
`CLAUDE.md` ecosystem-compatibility alias (Claude Code).
