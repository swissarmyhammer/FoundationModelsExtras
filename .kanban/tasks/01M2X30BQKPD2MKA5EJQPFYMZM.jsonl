{"id":"01M2X30BQKXHDN3H9WK79ANKFQ","timestamp":"2026-09-19T15:01:55.059766Z","op":"create","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,45 @@\n+---\n+depends_on:\n+- 01M2X2EMGFB08X9F0XV40N54DH\n+position_column: todo\n+position_ordinal: '8280'\n+title: 'Add a dotfolder document facade: read one path through the stack and split it'\n+---\n+## What\n+\n+A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer must repeat the same steps.\n+\n+Add the second layer of the stack: a document facade over the combined view of card ^40n54dh.\n+\n+1. New type `DotfolderDocument`:\n+   - `public var url: URL` — the file that won.\n+   - `public var layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust of the render and for its own provenance.\n+   - `public var frontmatterText: String?` — the raw text between the fences, or `nil`.\n+   - `public var body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n+2. New functions on `DotfolderStack`:\n+   - `public func document(at relativePath: String) -> DotfolderDocument?` — the winning copy of `relativePath`, read and split. `nil` when no layer holds it, or when the text is not UTF-8.\n+   - `public func documents(in subdirectory: String? = nil, named fileName: String) -> [String: DotfolderDocument]` — for each child directory of the union that holds `fileName`, the document of that file. The key is the name of the child directory. This is what a skill catalog needs: one call gives each `<id>/SKILL.md`.\n+3. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n+4. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way that a consumer must use.\n+\n+## Acceptance Criteria\n+\n+- [ ] `document(at:)` gives the copy of the highest layer, with the layer that gave it.\n+- [ ] `documents(in:named:)` gives one document for each child directory that holds the named file, and it skips a directory that does not hold it.\n+- [ ] A file that is not valid UTF-8 gives `nil`, not an error.\n+- [ ] A file with no frontmatter gives `frontmatterText == nil` and the full text as the body.\n+- [ ] The body is equal to the text after the fence, byte for byte.\n+- [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n+\n+## Tests\n+\n+- [ ] `Tests/FoundationModelsExtrasTests/DotfolderDocumentTests.swift` (new): two layers hold the same path; `document(at:)` gives the higher copy and names its layer.\n+- [ ] Same file: `documents(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file, and no entry for a directory that does not.\n+- [ ] Same file: a document with no frontmatter, a document with an unterminated fence, and a document with an empty frontmatter block each give the correct split.\n+- [ ] Same file: a file that is not valid UTF-8 gives `nil`.\n+- [ ] `swift test` — all tests pass, 0 failures.\n+\n+## Workflow\n+- Use `/tdd` — write failing tests first, then implement to make them pass.\n+\n+#dotfolder-overlay #cross-repo\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,45 +0,0 @@\n----\n-depends_on:\n-- 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: todo\n-position_ordinal: '8280'\n-title: 'Add a dotfolder document facade: read one path through the stack and split it'\n----\n-## What\n-\n-A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer must repeat the same steps.\n-\n-Add the second layer of the stack: a document facade over the combined view of card ^40n54dh.\n-\n-1. New type `DotfolderDocument`:\n-   - `public var url: URL` — the file that won.\n-   - `public var layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust of the render and for its own provenance.\n-   - `public var frontmatterText: String?` — the raw text between the fences, or `nil`.\n-   - `public var body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n-2. New functions on `DotfolderStack`:\n-   - `public func document(at relativePath: String) -> DotfolderDocument?` — the winning copy of `relativePath`, read and split. `nil` when no layer holds it, or when the text is not UTF-8.\n-   - `public func documents(in subdirectory: String? = nil, named fileName: String) -> [String: DotfolderDocument]` — for each child directory of the union that holds `fileName`, the document of that file. The key is the name of the child directory. This is what a skill catalog needs: one call gives each `<id>/SKILL.md`.\n-3. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n-4. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way that a consumer must use.\n-\n-## Acceptance Criteria\n-\n-- [ ] `document(at:)` gives the copy of the highest layer, with the layer that gave it.\n-- [ ] `documents(in:named:)` gives one document for each child directory that holds the named file, and it skips a directory that does not hold it.\n-- [ ] A file that is not valid UTF-8 gives `nil`, not an error.\n-- [ ] A file with no frontmatter gives `frontmatterText == nil` and the full text as the body.\n-- [ ] The body is equal to the text after the fence, byte for byte.\n-- [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n-\n-## Tests\n-\n-- [ ] `Tests/FoundationModelsExtrasTests/DotfolderDocumentTests.swift` (new): two layers hold the same path; `document(at:)` gives the higher copy and names its layer.\n-- [ ] Same file: `documents(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file, and no entry for a directory that does not.\n-- [ ] Same file: a document with no frontmatter, a document with an unterminated fence, and a document with an empty frontmatter block each give the correct split.\n-- [ ] Same file: a file that is not valid UTF-8 gives `nil`.\n-- [ ] `swift test` — all tests pass, 0 failures.\n-\n-## Workflow\n-- Use `/tdd` — write failing tests first, then implement to make them pass.\n-\n-#dotfolder-overlay #cross-repo\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M2X33V50TT6SRV4DTT20AFH3","timestamp":"2026-09-19T15:03:49.152859Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -3,32 +3,50 @@\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: todo\n position_ordinal: '8280'\n-title: 'Add a dotfolder document facade: read one path through the stack and split it'\n+title: 'Make the stack an interface: DotfolderStacking, documents, and the only file access'\n ---\n ## What\n\n-A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer must repeat the same steps.\n+A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer repeats the same steps.\n\n-Add the second layer of the stack: a document facade over the combined view of card ^40n54dh.\n+Make the stack an **interface**, so that more than one implementation can give the same view. This card gives the interface and the plain implementation. The card ^cwm3mqt gives the stenciled implementation.\n\n-1. New type `DotfolderDocument`:\n-   - `public var url: URL` — the file that won.\n-   - `public var layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust of the render and for its own provenance.\n-   - `public var frontmatterText: String?` — the raw text between the fences, or `nil`.\n-   - `public var body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n-2. New functions on `DotfolderStack`:\n-   - `public func document(at relativePath: String) -> DotfolderDocument?` — the winning copy of `relativePath`, read and split. `nil` when no layer holds it, or when the text is not UTF-8.\n-   - `public func documents(in subdirectory: String? = nil, named fileName: String) -> [String: DotfolderDocument]` — for each child directory of the union that holds `fileName`, the document of that file. The key is the name of the child directory. This is what a skill catalog needs: one call gives each `<id>/SKILL.md`.\n-3. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n-4. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way that a consumer must use.\n+1. **New protocol `DotfolderStacking`.** It holds each lookup of the combined view:\n+   - `var layers: [DotfolderStack.Layer] { get }`\n+   - `func nearest(_ relativePath: String) -> URL?`\n+   - `func locate(_ relativePath: String) -> [URL]`\n+   - `func content(_ relativePath: String) -> String?`\n+   - `func tree(_ subdirectory: String?) -> [String: DotfolderStack.Located]`\n+   - `func childDirectories(of subdirectory: String?) -> [String: [DotfolderStack.Layer]]`\n+   - `func layerDirectories(_ relativeDirectory: String?) -> [DotfolderStack.Layer]`\n+   - `func document(at relativePath: String) -> DotfolderDocument?`\n+   - `func documents(in subdirectory: String?, named fileName: String) -> [String: DotfolderDocument]`\n+   - `func data(_ relativePath: String) -> Data?` — the bytes of the winning copy, for a file that is not text.\n+   - `func data(_ relativePath: String, in range: Range<Int>) -> Data?` — a part of the winning copy, for a consumer that pages a large file.\n+   - `func size(of relativePath: String) -> Int?` — the byte count of the winning copy.\n+   - `func exists(_ relativePath: String) -> Bool`\n+2. **`DotfolderStack` conforms**, with no change of its behavior.\n+2a. **The stack is the only thing that opens a file.** A consumer must never need `FileManager`. Thus each lookup above resolves the symbolic links of the candidate and refuses a path that leaves its layer root, in addition to the text check of `isSafeRelativePath`. A path that escapes gives `nil` (or `false`, or an empty result), never a URL. `FoundationModelsSkills` holds this symbolic-link check today in `PathConfinement`; move that rule here.\n+3. **New type `DotfolderDocument`**, the value that the two document functions give:\n+   - `url: URL` — the file that won.\n+   - `layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust and for its own provenance.\n+   - `frontmatterText: String?` — the raw text between the fences, or `nil`.\n+   - `body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n+   - `func text(of value: String) -> String` — gives one value back. The plain implementation gives it unchanged; the stenciled implementation renders it. A consumer uses this for one value of the frontmatter, for example a `description`.\n+4. `documents(in:named:)` gives, for each child directory of the union that holds `fileName`, the document of that file, keyed by the name of the child directory. One call gives each `<id>/SKILL.md`.\n+5. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n+6. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way a consumer must use.\n\n ## Acceptance Criteria\n\n+- [ ] `DotfolderStacking` holds each lookup, and `DotfolderStack` conforms with no change of behavior.\n - [ ] `document(at:)` gives the copy of the highest layer, with the layer that gave it.\n - [ ] `documents(in:named:)` gives one document for each child directory that holds the named file, and it skips a directory that does not hold it.\n - [ ] A file that is not valid UTF-8 gives `nil`, not an error.\n - [ ] A file with no frontmatter gives `frontmatterText == nil` and the full text as the body.\n - [ ] The body is equal to the text after the fence, byte for byte.\n+- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view: the highest layer that holds the path wins.\n+- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup of the protocol.\n - [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n\n ## Tests\n@@ -37,6 +55,7 @@\n - [ ] Same file: `documents(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file, and no entry for a directory that does not.\n - [ ] Same file: a document with no frontmatter, a document with an unterminated fence, and a document with an empty frontmatter block each give the correct split.\n - [ ] Same file: a file that is not valid UTF-8 gives `nil`.\n+- [ ] Same file: `text(of:)` of the plain implementation gives the value unchanged.\n - [ ] `swift test` — all tests pass, 0 failures.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -3,50 +3,32 @@\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: todo\n position_ordinal: '8280'\n-title: 'Make the stack an interface: DotfolderStacking, documents, and the only file access'\n+title: 'Add a dotfolder document facade: read one path through the stack and split it'\n ---\n ## What\n\n-A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer repeats the same steps.\n+A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer must repeat the same steps.\n\n-Make the stack an **interface**, so that more than one implementation can give the same view. This card gives the interface and the plain implementation. The card ^cwm3mqt gives the stenciled implementation.\n+Add the second layer of the stack: a document facade over the combined view of card ^40n54dh.\n\n-1. **New protocol `DotfolderStacking`.** It holds each lookup of the combined view:\n-   - `var layers: [DotfolderStack.Layer] { get }`\n-   - `func nearest(_ relativePath: String) -> URL?`\n-   - `func locate(_ relativePath: String) -> [URL]`\n-   - `func content(_ relativePath: String) -> String?`\n-   - `func tree(_ subdirectory: String?) -> [String: DotfolderStack.Located]`\n-   - `func childDirectories(of subdirectory: String?) -> [String: [DotfolderStack.Layer]]`\n-   - `func layerDirectories(_ relativeDirectory: String?) -> [DotfolderStack.Layer]`\n-   - `func document(at relativePath: String) -> DotfolderDocument?`\n-   - `func documents(in subdirectory: String?, named fileName: String) -> [String: DotfolderDocument]`\n-   - `func data(_ relativePath: String) -> Data?` — the bytes of the winning copy, for a file that is not text.\n-   - `func data(_ relativePath: String, in range: Range<Int>) -> Data?` — a part of the winning copy, for a consumer that pages a large file.\n-   - `func size(of relativePath: String) -> Int?` — the byte count of the winning copy.\n-   - `func exists(_ relativePath: String) -> Bool`\n-2. **`DotfolderStack` conforms**, with no change of its behavior.\n-2a. **The stack is the only thing that opens a file.** A consumer must never need `FileManager`. Thus each lookup above resolves the symbolic links of the candidate and refuses a path that leaves its layer root, in addition to the text check of `isSafeRelativePath`. A path that escapes gives `nil` (or `false`, or an empty result), never a URL. `FoundationModelsSkills` holds this symbolic-link check today in `PathConfinement`; move that rule here.\n-3. **New type `DotfolderDocument`**, the value that the two document functions give:\n-   - `url: URL` — the file that won.\n-   - `layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust and for its own provenance.\n-   - `frontmatterText: String?` — the raw text between the fences, or `nil`.\n-   - `body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n-   - `func text(of value: String) -> String` — gives one value back. The plain implementation gives it unchanged; the stenciled implementation renders it. A consumer uses this for one value of the frontmatter, for example a `description`.\n-4. `documents(in:named:)` gives, for each child directory of the union that holds `fileName`, the document of that file, keyed by the name of the child directory. One call gives each `<id>/SKILL.md`.\n-5. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n-6. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way a consumer must use.\n+1. New type `DotfolderDocument`:\n+   - `public var url: URL` — the file that won.\n+   - `public var layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust of the render and for its own provenance.\n+   - `public var frontmatterText: String?` — the raw text between the fences, or `nil`.\n+   - `public var body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n+2. New functions on `DotfolderStack`:\n+   - `public func document(at relativePath: String) -> DotfolderDocument?` — the winning copy of `relativePath`, read and split. `nil` when no layer holds it, or when the text is not UTF-8.\n+   - `public func documents(in subdirectory: String? = nil, named fileName: String) -> [String: DotfolderDocument]` — for each child directory of the union that holds `fileName`, the document of that file. The key is the name of the child directory. This is what a skill catalog needs: one call gives each `<id>/SKILL.md`.\n+3. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n+4. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way that a consumer must use.\n\n ## Acceptance Criteria\n\n-- [ ] `DotfolderStacking` holds each lookup, and `DotfolderStack` conforms with no change of behavior.\n - [ ] `document(at:)` gives the copy of the highest layer, with the layer that gave it.\n - [ ] `documents(in:named:)` gives one document for each child directory that holds the named file, and it skips a directory that does not hold it.\n - [ ] A file that is not valid UTF-8 gives `nil`, not an error.\n - [ ] A file with no frontmatter gives `frontmatterText == nil` and the full text as the body.\n - [ ] The body is equal to the text after the fence, byte for byte.\n-- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view: the highest layer that holds the path wins.\n-- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup of the protocol.\n - [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n\n ## Tests\n@@ -55,7 +37,6 @@\n - [ ] Same file: `documents(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file, and no entry for a directory that does not.\n - [ ] Same file: a document with no frontmatter, a document with an unterminated fence, and a document with an empty frontmatter block each give the correct split.\n - [ ] Same file: a file that is not valid UTF-8 gives `nil`.\n-- [ ] Same file: `text(of:)` of the plain implementation gives the value unchanged.\n - [ ] `swift test` — all tests pass, 0 failures.\n\n ## Workflow\n","transaction_id":null}
{"id":"01M2X373SRFQ4E24C7AP5K4FHM","timestamp":"2026-09-19T15:05:36.312362Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -3,59 +3,52 @@\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: todo\n position_ordinal: '8280'\n-title: 'Make the stack an interface: DotfolderStacking, documents, and the only file access'\n+title: Make DotfolderStacking a generic interface, and the only file access\n ---\n ## What\n\n-A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer repeats the same steps.\n+Make the stack an **interface**, and make the interface generic in what one lookup gives back. The lower layers give content. A higher layer gives metadata with the content. Each layer has the same shape, thus a consumer composes the layers that it needs.\n+\n+```\n+DotfolderStack                Item = String          find and read\n+StenciledDotfolderStack       Item = String          the same, rendered     (card ^cwm3mqt)\n+FrontmatterDocumentStack      Item = (metadata, content)                    (card ^EXTRAS5)\n+```\n\n-Make the stack an **interface**, so that more than one implementation can give the same view. This card gives the interface and the plain implementation. The card ^cwm3mqt gives the stenciled implementation.\n+This card gives the interface and the plain implementation.\n\n-1. **New protocol `DotfolderStacking`.** It holds each lookup of the combined view:\n+1. **New protocol `DotfolderStacking`**, with `associatedtype Item`:\n    - `var layers: [DotfolderStack.Layer] { get }`\n-   - `func nearest(_ relativePath: String) -> URL?`\n-   - `func locate(_ relativePath: String) -> [URL]`\n-   - `func content(_ relativePath: String) -> String?`\n-   - `func tree(_ subdirectory: String?) -> [String: DotfolderStack.Located]`\n+   - `func item(at relativePath: String) -> Located<Item>?` — the winning copy, with the layer that gave it.\n+   - `func items(in subdirectory: String?, named fileName: String) -> [String: Located<Item>]` — for each child directory of the union that holds `fileName`, that file. The key is the name of the child directory. One call gives each `<id>/SKILL.md`.\n+   - `func tree(_ subdirectory: String?) -> [String: Located<Item>]` — the recursive combined view.\n    - `func childDirectories(of subdirectory: String?) -> [String: [DotfolderStack.Layer]]`\n    - `func layerDirectories(_ relativeDirectory: String?) -> [DotfolderStack.Layer]`\n-   - `func document(at relativePath: String) -> DotfolderDocument?`\n-   - `func documents(in subdirectory: String?, named fileName: String) -> [String: DotfolderDocument]`\n    - `func data(_ relativePath: String) -> Data?` — the bytes of the winning copy, for a file that is not text.\n    - `func data(_ relativePath: String, in range: Range<Int>) -> Data?` — a part of the winning copy, for a consumer that pages a large file.\n-   - `func size(of relativePath: String) -> Int?` — the byte count of the winning copy.\n+   - `func size(of relativePath: String) -> Int?`\n    - `func exists(_ relativePath: String) -> Bool`\n-2. **`DotfolderStack` conforms**, with no change of its behavior.\n-2a. **The stack is the only thing that opens a file.** A consumer must never need `FileManager`. Thus each lookup above resolves the symbolic links of the candidate and refuses a path that leaves its layer root, in addition to the text check of `isSafeRelativePath`. A path that escapes gives `nil` (or `false`, or an empty result), never a URL. `FoundationModelsSkills` holds this symbolic-link check today in `PathConfinement`; move that rule here.\n-3. **New type `DotfolderDocument`**, the value that the two document functions give:\n-   - `url: URL` — the file that won.\n-   - `layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust and for its own provenance.\n-   - `frontmatterText: String?` — the raw text between the fences, or `nil`.\n-   - `body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n-   - `func text(of value: String) -> String` — gives one value back. The plain implementation gives it unchanged; the stenciled implementation renders it. A consumer uses this for one value of the frontmatter, for example a `description`.\n-4. `documents(in:named:)` gives, for each child directory of the union that holds `fileName`, the document of that file, keyed by the name of the child directory. One call gives each `<id>/SKILL.md`.\n-5. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n-6. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way a consumer must use.\n+2. **`Located<Item>`** becomes generic: `url`, `layer` and `value: Item`. The layer is necessary at each level: a consumer needs it for the trust and for its own provenance.\n+3. **`DotfolderStack` conforms with `Item == String`**, and its behavior does not change. `nearest`, `locate`, `content` and `enumerate` stay as they are.\n+4. **The stack is the only thing that opens a file.** A consumer must never need `FileManager`. Thus each lookup resolves the symbolic links of the candidate and refuses a path that leaves its layer root, in addition to the text check of `isSafeRelativePath`. A path that escapes gives `nil`, `false` or an empty result, never a URL. `FoundationModelsSkills` holds this symbolic-link check today in `PathConfinement`; move that rule here.\n+5. A decorator gets its files from its base, thus only `DotfolderStack` touches the disk.\n\n ## Acceptance Criteria\n\n-- [ ] `DotfolderStacking` holds each lookup, and `DotfolderStack` conforms with no change of behavior.\n-- [ ] `document(at:)` gives the copy of the highest layer, with the layer that gave it.\n-- [ ] `documents(in:named:)` gives one document for each child directory that holds the named file, and it skips a directory that does not hold it.\n-- [ ] A file that is not valid UTF-8 gives `nil`, not an error.\n-- [ ] A file with no frontmatter gives `frontmatterText == nil` and the full text as the body.\n-- [ ] The body is equal to the text after the fence, byte for byte.\n-- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view: the highest layer that holds the path wins.\n-- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup of the protocol.\n+- [ ] `DotfolderStacking` is generic in `Item`, and `DotfolderStack` conforms with `Item == String` and no change of behavior.\n+- [ ] `item(at:)` gives the copy of the highest layer, with the layer that gave it.\n+- [ ] `items(in:named:)` gives one entry for each child directory that holds the named file, and it skips a directory that does not hold it.\n+- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view.\n+- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup.\n+- [ ] A file that is not valid UTF-8 gives `nil` from `item(at:)`, and its bytes from `data(_:)`.\n - [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n\n ## Tests\n\n-- [ ] `Tests/FoundationModelsExtrasTests/DotfolderDocumentTests.swift` (new): two layers hold the same path; `document(at:)` gives the higher copy and names its layer.\n-- [ ] Same file: `documents(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file, and no entry for a directory that does not.\n-- [ ] Same file: a document with no frontmatter, a document with an unterminated fence, and a document with an empty frontmatter block each give the correct split.\n-- [ ] Same file: a file that is not valid UTF-8 gives `nil`.\n-- [ ] Same file: `text(of:)` of the plain implementation gives the value unchanged.\n+- [ ] `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new): two layers hold the same path; `item(at:)` gives the higher copy and names its layer.\n+- [ ] Same file: `items(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file.\n+- [ ] Same file: a symbolic link that points outside the layer root gives no result from `item(at:)`, `data(_:)` and `exists(_:)`.\n+- [ ] Same file: the ranged `data(_:in:)` gives the same bytes as the full read for that range.\n - [ ] `swift test` — all tests pass, 0 failures.\n\n ## Workflow\n","reverse_patch":"--- original\n+++ modified\n@@ -3,52 +3,59 @@\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: todo\n position_ordinal: '8280'\n-title: Make DotfolderStacking a generic interface, and the only file access\n+title: 'Make the stack an interface: DotfolderStacking, documents, and the only file access'\n ---\n ## What\n\n-Make the stack an **interface**, and make the interface generic in what one lookup gives back. The lower layers give content. A higher layer gives metadata with the content. Each layer has the same shape, thus a consumer composes the layers that it needs.\n+A consumer must not read a dotfolder file by itself. Today `FoundationModelsSkills` opens `SKILL.md` with `FileManager`, then calls `FrontmatterDocument.split` by itself, then goes back to its own code. The layers are crossed two times, and each consumer repeats the same steps.\n\n-```\n-DotfolderStack                Item = String          find and read\n-StenciledDotfolderStack       Item = String          the same, rendered     (card ^cwm3mqt)\n-FrontmatterDocumentStack      Item = (metadata, content)                    (card ^EXTRAS5)\n-```\n+Make the stack an **interface**, so that more than one implementation can give the same view. This card gives the interface and the plain implementation. The card ^cwm3mqt gives the stenciled implementation.\n\n-This card gives the interface and the plain implementation.\n-\n-1. **New protocol `DotfolderStacking`**, with `associatedtype Item`:\n+1. **New protocol `DotfolderStacking`.** It holds each lookup of the combined view:\n    - `var layers: [DotfolderStack.Layer] { get }`\n-   - `func item(at relativePath: String) -> Located<Item>?` — the winning copy, with the layer that gave it.\n-   - `func items(in subdirectory: String?, named fileName: String) -> [String: Located<Item>]` — for each child directory of the union that holds `fileName`, that file. The key is the name of the child directory. One call gives each `<id>/SKILL.md`.\n-   - `func tree(_ subdirectory: String?) -> [String: Located<Item>]` — the recursive combined view.\n+   - `func nearest(_ relativePath: String) -> URL?`\n+   - `func locate(_ relativePath: String) -> [URL]`\n+   - `func content(_ relativePath: String) -> String?`\n+   - `func tree(_ subdirectory: String?) -> [String: DotfolderStack.Located]`\n    - `func childDirectories(of subdirectory: String?) -> [String: [DotfolderStack.Layer]]`\n    - `func layerDirectories(_ relativeDirectory: String?) -> [DotfolderStack.Layer]`\n+   - `func document(at relativePath: String) -> DotfolderDocument?`\n+   - `func documents(in subdirectory: String?, named fileName: String) -> [String: DotfolderDocument]`\n    - `func data(_ relativePath: String) -> Data?` — the bytes of the winning copy, for a file that is not text.\n    - `func data(_ relativePath: String, in range: Range<Int>) -> Data?` — a part of the winning copy, for a consumer that pages a large file.\n-   - `func size(of relativePath: String) -> Int?`\n+   - `func size(of relativePath: String) -> Int?` — the byte count of the winning copy.\n    - `func exists(_ relativePath: String) -> Bool`\n-2. **`Located<Item>`** becomes generic: `url`, `layer` and `value: Item`. The layer is necessary at each level: a consumer needs it for the trust and for its own provenance.\n-3. **`DotfolderStack` conforms with `Item == String`**, and its behavior does not change. `nearest`, `locate`, `content` and `enumerate` stay as they are.\n-4. **The stack is the only thing that opens a file.** A consumer must never need `FileManager`. Thus each lookup resolves the symbolic links of the candidate and refuses a path that leaves its layer root, in addition to the text check of `isSafeRelativePath`. A path that escapes gives `nil`, `false` or an empty result, never a URL. `FoundationModelsSkills` holds this symbolic-link check today in `PathConfinement`; move that rule here.\n-5. A decorator gets its files from its base, thus only `DotfolderStack` touches the disk.\n+2. **`DotfolderStack` conforms**, with no change of its behavior.\n+2a. **The stack is the only thing that opens a file.** A consumer must never need `FileManager`. Thus each lookup above resolves the symbolic links of the candidate and refuses a path that leaves its layer root, in addition to the text check of `isSafeRelativePath`. A path that escapes gives `nil` (or `false`, or an empty result), never a URL. `FoundationModelsSkills` holds this symbolic-link check today in `PathConfinement`; move that rule here.\n+3. **New type `DotfolderDocument`**, the value that the two document functions give:\n+   - `url: URL` — the file that won.\n+   - `layer: DotfolderStack.Layer` — the layer that gave it. A consumer needs it for the trust and for its own provenance.\n+   - `frontmatterText: String?` — the raw text between the fences, or `nil`.\n+   - `body: String` — the text after the closing fence, byte for byte. The full text when there is no frontmatter block.\n+   - `func text(of value: String) -> String` — gives one value back. The plain implementation gives it unchanged; the stenciled implementation renders it. A consumer uses this for one value of the frontmatter, for example a `description`.\n+4. `documents(in:named:)` gives, for each child directory of the union that holds `fileName`, the document of that file, keyed by the name of the child directory. One call gives each `<id>/SKILL.md`.\n+5. `DotfolderDocument` holds no YAML. The decode of the frontmatter text is the work of the consumer, with its own codec.\n+6. `FrontmatterDocument` stays public, but the documents state that `document(at:)` is the way a consumer must use.\n\n ## Acceptance Criteria\n\n-- [ ] `DotfolderStacking` is generic in `Item`, and `DotfolderStack` conforms with `Item == String` and no change of behavior.\n-- [ ] `item(at:)` gives the copy of the highest layer, with the layer that gave it.\n-- [ ] `items(in:named:)` gives one entry for each child directory that holds the named file, and it skips a directory that does not hold it.\n-- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view.\n-- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup.\n-- [ ] A file that is not valid UTF-8 gives `nil` from `item(at:)`, and its bytes from `data(_:)`.\n+- [ ] `DotfolderStacking` holds each lookup, and `DotfolderStack` conforms with no change of behavior.\n+- [ ] `document(at:)` gives the copy of the highest layer, with the layer that gave it.\n+- [ ] `documents(in:named:)` gives one document for each child directory that holds the named file, and it skips a directory that does not hold it.\n+- [ ] A file that is not valid UTF-8 gives `nil`, not an error.\n+- [ ] A file with no frontmatter gives `frontmatterText == nil` and the full text as the body.\n+- [ ] The body is equal to the text after the fence, byte for byte.\n+- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view: the highest layer that holds the path wins.\n+- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup of the protocol.\n - [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n\n ## Tests\n\n-- [ ] `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new): two layers hold the same path; `item(at:)` gives the higher copy and names its layer.\n-- [ ] Same file: `items(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file.\n-- [ ] Same file: a symbolic link that points outside the layer root gives no result from `item(at:)`, `data(_:)` and `exists(_:)`.\n-- [ ] Same file: the ranged `data(_:in:)` gives the same bytes as the full read for that range.\n+- [ ] `Tests/FoundationModelsExtrasTests/DotfolderDocumentTests.swift` (new): two layers hold the same path; `document(at:)` gives the higher copy and names its layer.\n+- [ ] Same file: `documents(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file, and no entry for a directory that does not.\n+- [ ] Same file: a document with no frontmatter, a document with an unterminated fence, and a document with an empty frontmatter block each give the correct split.\n+- [ ] Same file: a file that is not valid UTF-8 gives `nil`.\n+- [ ] Same file: `text(of:)` of the plain implementation gives the value unchanged.\n - [ ] `swift test` — all tests pass, 0 failures.\n\n ## Workflow\n","transaction_id":null}
{"id":"01M2X37C6NEZD6BGTMZR1FMN1W","timestamp":"2026-09-19T15:05:44.917020Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -12,7 +12,7 @@\n ```\n DotfolderStack                Item = String          find and read\n StenciledDotfolderStack       Item = String          the same, rendered     (card ^cwm3mqt)\n-FrontmatterDocumentStack      Item = (metadata, content)                    (card ^EXTRAS5)\n+FrontmatterDocumentStack      Item = (metadata, content)                    (card ^9ta9smb)\n ```\n\n This card gives the interface and the plain implementation.\n","reverse_patch":"--- original\n+++ modified\n@@ -12,7 +12,7 @@\n ```\n DotfolderStack                Item = String          find and read\n StenciledDotfolderStack       Item = String          the same, rendered     (card ^cwm3mqt)\n-FrontmatterDocumentStack      Item = (metadata, content)                    (card ^9ta9smb)\n+FrontmatterDocumentStack      Item = (metadata, content)                    (card ^EXTRAS5)\n ```\n\n This card gives the interface and the plain implementation.\n","transaction_id":null}
{"id":"01M2X3ETGPCJA3PC8ME6SXPA3P","timestamp":"2026-09-19T15:09:48.950604Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: todo\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n title: Make DotfolderStacking a generic interface, and the only file access\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8280'\n title: Make DotfolderStacking a generic interface, and the only file access\n ---\n ## What\n","transaction_id":null}
{"id":"01M2X3M8MD30Y4Q593A91KT2EX","timestamp":"2026-09-19T15:12:47.245757Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -1,4 +1,23 @@\n ---\n+comments:\n+- actor: claude-code\n+  id: 01m2x3m8mdwsy2xp1a18mbq6ne\n+  text: |-\n+    Picked up. Research:\n+\n+    - `Sources/FoundationModelsExtras/DotfolderStack.swift` holds `DotfolderStack`, `Layer`, and a non-generic `Located` (url, layer). Lookups: `nearest`, `content`, `locate`, `enumerate`, `tree`, `childDirectories`, `layerDirectories`. Each applies the text check `isSafeRelativePath` only. No symbolic-link check exists.\n+    - Callers of `Located` in this package: `DotfolderStack.swift`, `Tests/.../DotfolderStackTests.swift` (`.url`, `.layer.source`), `Examples/ExtrasDemo/.../StackCommand.swift` (`entry.layer.source`). All of them keep to work with a generic `Located<Item>` that keeps `url` and `layer`.\n+    - `LayeredYAMLDocument.swift` and `DotfolderLoader.swift` call `locate` and `content` only. They do not change.\n+    - `FoundationModelsSkills/Sources/FoundationModelsSkills/Resources/PathConfinement.swift` holds the symbolic-link rule: resolve the symbolic links of the longest prefix of the candidate that exists, resolve the root, and accept only a candidate that is the root or under the root. The card says to move that rule here. This card cannot edit the Skills repository; the rule lands in this package as `Sources/FoundationModelsExtras/PathConfinement.swift`.\n+    - The existing test `aLayerRootThatIsASymbolicLinkIsWalked` expects the `url` of a located item to be the joined path (`<root>/review/SKILL.md`), not the resolved path. Thus the resolution serves the containment check only; `url` stays the joined path.\n+    - No `ARCHITECTURE.md` exists in the package.\n+\n+    Design:\n+    - `Located<Item>` becomes a top-level generic struct (`url`, `layer`, `value`), `Sendable` when `Item` is.\n+    - `DotfolderStacking` protocol with `associatedtype Item: Sendable` and the functions from the card. `DotfolderStack` conforms with `Item == String`.\n+    - `enumerate` and `tree` give `Located<String>`, thus they read the text of each winning file. A file that is not valid UTF-8 is not in that view; `data(_:)` gives its bytes.\n+    - Test file: `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift`, reusing `DotfolderStackTests.Fixture`.\n+  timestamp: 2026-09-19T15:12:47.245398+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -1,23 +1,4 @@\n ---\n-comments:\n-- actor: claude-code\n-  id: 01m2x3m8mdwsy2xp1a18mbq6ne\n-  text: |-\n-    Picked up. Research:\n-\n-    - `Sources/FoundationModelsExtras/DotfolderStack.swift` holds `DotfolderStack`, `Layer`, and a non-generic `Located` (url, layer). Lookups: `nearest`, `content`, `locate`, `enumerate`, `tree`, `childDirectories`, `layerDirectories`. Each applies the text check `isSafeRelativePath` only. No symbolic-link check exists.\n-    - Callers of `Located` in this package: `DotfolderStack.swift`, `Tests/.../DotfolderStackTests.swift` (`.url`, `.layer.source`), `Examples/ExtrasDemo/.../StackCommand.swift` (`entry.layer.source`). All of them keep to work with a generic `Located<Item>` that keeps `url` and `layer`.\n-    - `LayeredYAMLDocument.swift` and `DotfolderLoader.swift` call `locate` and `content` only. They do not change.\n-    - `FoundationModelsSkills/Sources/FoundationModelsSkills/Resources/PathConfinement.swift` holds the symbolic-link rule: resolve the symbolic links of the longest prefix of the candidate that exists, resolve the root, and accept only a candidate that is the root or under the root. The card says to move that rule here. This card cannot edit the Skills repository; the rule lands in this package as `Sources/FoundationModelsExtras/PathConfinement.swift`.\n-    - The existing test `aLayerRootThatIsASymbolicLinkIsWalked` expects the `url` of a located item to be the joined path (`<root>/review/SKILL.md`), not the resolved path. Thus the resolution serves the containment check only; `url` stays the joined path.\n-    - No `ARCHITECTURE.md` exists in the package.\n-\n-    Design:\n-    - `Located<Item>` becomes a top-level generic struct (`url`, `layer`, `value`), `Sendable` when `Item` is.\n-    - `DotfolderStacking` protocol with `associatedtype Item: Sendable` and the functions from the card. `DotfolderStack` conforms with `Item == String`.\n-    - `enumerate` and `tree` give `Located<String>`, thus they read the text of each winning file. A file that is not valid UTF-8 is not in that view; `data(_:)` gives its bytes.\n-    - Test file: `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift`, reusing `DotfolderStackTests.Fixture`.\n-  timestamp: 2026-09-19T15:12:47.245398+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","transaction_id":null}
{"id":"01M2X437KRPHE3YB8PD8A50A8H","timestamp":"2026-09-19T15:20:57.720710Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -54,21 +54,21 @@\n\n ## Acceptance Criteria\n\n-- [ ] `DotfolderStacking` is generic in `Item`, and `DotfolderStack` conforms with `Item == String` and no change of behavior.\n-- [ ] `item(at:)` gives the copy of the highest layer, with the layer that gave it.\n-- [ ] `items(in:named:)` gives one entry for each child directory that holds the named file, and it skips a directory that does not hold it.\n-- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view.\n-- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup.\n-- [ ] A file that is not valid UTF-8 gives `nil` from `item(at:)`, and its bytes from `data(_:)`.\n-- [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n+- [x] `DotfolderStacking` is generic in `Item`, and `DotfolderStack` conforms with `Item == String` and no change of behavior.\n+- [x] `item(at:)` gives the copy of the highest layer, with the layer that gave it.\n+- [x] `items(in:named:)` gives one entry for each child directory that holds the named file, and it skips a directory that does not hold it.\n+- [x] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view.\n+- [x] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup.\n+- [x] A file that is not valid UTF-8 gives `nil` from `item(at:)`, and its bytes from `data(_:)`.\n+- [x] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n\n ## Tests\n\n-- [ ] `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new): two layers hold the same path; `item(at:)` gives the higher copy and names its layer.\n-- [ ] Same file: `items(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file.\n-- [ ] Same file: a symbolic link that points outside the layer root gives no result from `item(at:)`, `data(_:)` and `exists(_:)`.\n-- [ ] Same file: the ranged `data(_:in:)` gives the same bytes as the full read for that range.\n-- [ ] `swift test` — all tests pass, 0 failures.\n+- [x] `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new): two layers hold the same path; `item(at:)` gives the higher copy and names its layer.\n+- [x] Same file: `items(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file.\n+- [x] Same file: a symbolic link that points outside the layer root gives no result from `item(at:)`, `data(_:)` and `exists(_:)`.\n+- [x] Same file: the ranged `data(_:in:)` gives the same bytes as the full read for that range.\n+- [x] `swift test` — all tests pass, 0 failures.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n","reverse_patch":"--- original\n+++ modified\n@@ -54,21 +54,21 @@\n\n ## Acceptance Criteria\n\n-- [x] `DotfolderStacking` is generic in `Item`, and `DotfolderStack` conforms with `Item == String` and no change of behavior.\n-- [x] `item(at:)` gives the copy of the highest layer, with the layer that gave it.\n-- [x] `items(in:named:)` gives one entry for each child directory that holds the named file, and it skips a directory that does not hold it.\n-- [x] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view.\n-- [x] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup.\n-- [x] A file that is not valid UTF-8 gives `nil` from `item(at:)`, and its bytes from `data(_:)`.\n-- [x] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n+- [ ] `DotfolderStacking` is generic in `Item`, and `DotfolderStack` conforms with `Item == String` and no change of behavior.\n+- [ ] `item(at:)` gives the copy of the highest layer, with the layer that gave it.\n+- [ ] `items(in:named:)` gives one entry for each child directory that holds the named file, and it skips a directory that does not hold it.\n+- [ ] `data(_:)`, the ranged form, `size(of:)` and `exists(_:)` all obey the combined view.\n+- [ ] A path that resolves through a symbolic link to a location outside its layer root gives no result, for each lookup.\n+- [ ] A file that is not valid UTF-8 gives `nil` from `item(at:)`, and its bytes from `data(_:)`.\n+- [ ] `swift build --build-tests` gives 0 warnings, and `swift test` is green.\n\n ## Tests\n\n-- [x] `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new): two layers hold the same path; `item(at:)` gives the higher copy and names its layer.\n-- [x] Same file: `items(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file.\n-- [x] Same file: a symbolic link that points outside the layer root gives no result from `item(at:)`, `data(_:)` and `exists(_:)`.\n-- [x] Same file: the ranged `data(_:in:)` gives the same bytes as the full read for that range.\n-- [x] `swift test` — all tests pass, 0 failures.\n+- [ ] `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new): two layers hold the same path; `item(at:)` gives the higher copy and names its layer.\n+- [ ] Same file: `items(in: nil, named: \"SKILL.md\")` gives one entry for each child directory that holds the file.\n+- [ ] Same file: a symbolic link that points outside the layer root gives no result from `item(at:)`, `data(_:)` and `exists(_:)`.\n+- [ ] Same file: the ranged `data(_:in:)` gives the same bytes as the full read for that range.\n+- [ ] `swift test` — all tests pass, 0 failures.\n\n ## Workflow\n - Use `/tdd` — write failing tests first, then implement to make them pass.\n","transaction_id":null}
{"id":"01M2X43R7P56V8V6AZ68N6Y2PR","timestamp":"2026-09-19T15:21:14.742377Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -18,6 +18,27 @@\n     - `enumerate` and `tree` give `Located<String>`, thus they read the text of each winning file. A file that is not valid UTF-8 is not in that view; `data(_:)` gives its bytes.\n     - Test file: `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift`, reusing `DotfolderStackTests.Fixture`.\n   timestamp: 2026-09-19T15:12:47.245398+00:00\n+- actor: claude-code\n+  id: 01m2x43r7pqsq7wsjm9czh36c8\n+  text: |-\n+    Implementation landed, with `/tdd`.\n+\n+    RED: `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new, 19 tests) did not compile: no `DotfolderStacking`, no `item`, `items`, `data`, `size`, `exists`, no `Located.value`.\n+\n+    GREEN:\n+    - `Sources/FoundationModelsExtras/DotfolderStacking.swift` (new): top-level generic `Located<Item: Sendable>` (`url`, `layer`, `value`) and the `DotfolderStacking` protocol with the functions from the card.\n+    - `Sources/FoundationModelsExtras/PathConfinement.swift` (new): the symbolic-link confinement rule from the Skills package. Both the root and the candidate go through the same \"resolve the symbolic links of the longest prefix that exists\" step, so a missing layer root and a candidate under it compare in the same form.\n+    - `Sources/FoundationModelsExtras/DotfolderStack.swift`: the nested `Located` is gone; `enumerate` and `tree` give `Located<String>` (thus they read the text of each winning file); `item(at:)`, `items(in:named:)`, `data`, ranged `data`, `size(of:)`, `exists` added; each lookup routes through `Layer.confines(_:)`. Each single-path lookup routes through `winningCopy`, each byte lookup through `readingWinningCopy`, and `tree`/`enumerate` through `winningTexts(of:)`, so the override rule and the two path checks live in one place each.\n+    - `Tests/FoundationModelsExtrasTests/DotfolderStackTests.swift`: the fixture got a `Data` overload of `write` and a `link` helper; the existing symbolic-link test uses the helper.\n+\n+    Discoveries:\n+    - `Located.url` stays the joined path (`<root>/review/SKILL.md`), never the resolved path, because the existing test on a symbolic-link root expects that. The resolution serves the containment check only.\n+    - `tree` and `enumerate` decide the winner first and read the text after, so a winning copy that is not UTF-8 text leaves the view instead of letting a lower copy show through. That keeps them consistent with `item(at:)`.\n+    - The `no_magic_numbers` rule does not exempt a Swift Testing `@Suite`, so the byte ranges in the new tests are named `static let`s.\n+    - The deletion of the Skills copy of `PathConfinement` is a change in another repository. Follow-up task added on this board, depends on this card.\n+\n+    Verification: `swift build --build-tests` gives 0 warnings; `swift test` gives 530 tests in 5 bundles, 0 failures.\n+  timestamp: 2026-09-19T15:21:14.742051+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -18,27 +18,6 @@\n     - `enumerate` and `tree` give `Located<String>`, thus they read the text of each winning file. A file that is not valid UTF-8 is not in that view; `data(_:)` gives its bytes.\n     - Test file: `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift`, reusing `DotfolderStackTests.Fixture`.\n   timestamp: 2026-09-19T15:12:47.245398+00:00\n-- actor: claude-code\n-  id: 01m2x43r7pqsq7wsjm9czh36c8\n-  text: |-\n-    Implementation landed, with `/tdd`.\n-\n-    RED: `Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift` (new, 19 tests) did not compile: no `DotfolderStacking`, no `item`, `items`, `data`, `size`, `exists`, no `Located.value`.\n-\n-    GREEN:\n-    - `Sources/FoundationModelsExtras/DotfolderStacking.swift` (new): top-level generic `Located<Item: Sendable>` (`url`, `layer`, `value`) and the `DotfolderStacking` protocol with the functions from the card.\n-    - `Sources/FoundationModelsExtras/PathConfinement.swift` (new): the symbolic-link confinement rule from the Skills package. Both the root and the candidate go through the same \"resolve the symbolic links of the longest prefix that exists\" step, so a missing layer root and a candidate under it compare in the same form.\n-    - `Sources/FoundationModelsExtras/DotfolderStack.swift`: the nested `Located` is gone; `enumerate` and `tree` give `Located<String>` (thus they read the text of each winning file); `item(at:)`, `items(in:named:)`, `data`, ranged `data`, `size(of:)`, `exists` added; each lookup routes through `Layer.confines(_:)`. Each single-path lookup routes through `winningCopy`, each byte lookup through `readingWinningCopy`, and `tree`/`enumerate` through `winningTexts(of:)`, so the override rule and the two path checks live in one place each.\n-    - `Tests/FoundationModelsExtrasTests/DotfolderStackTests.swift`: the fixture got a `Data` overload of `write` and a `link` helper; the existing symbolic-link test uses the helper.\n-\n-    Discoveries:\n-    - `Located.url` stays the joined path (`<root>/review/SKILL.md`), never the resolved path, because the existing test on a symbolic-link root expects that. The resolution serves the containment check only.\n-    - `tree` and `enumerate` decide the winner first and read the text after, so a winning copy that is not UTF-8 text leaves the view instead of letting a lower copy show through. That keeps them consistent with `item(at:)`.\n-    - The `no_magic_numbers` rule does not exempt a Swift Testing `@Suite`, so the byte ranges in the new tests are named `static let`s.\n-    - The deletion of the Skills copy of `PathConfinement` is a change in another repository. Follow-up task added on this board, depends on this card.\n-\n-    Verification: `swift build --build-tests` gives 0 warnings; `swift test` gives 530 tests in 5 bundles, 0 failures.\n-  timestamp: 2026-09-19T15:21:14.742051+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","transaction_id":null}
{"id":"01M2X43ZXKCEM6DA7W5DBD7FN9","timestamp":"2026-09-19T15:21:22.611429Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -39,6 +39,13 @@\n\n     Verification: `swift build --build-tests` gives 0 warnings; `swift test` gives 530 tests in 5 bundles, 0 failures.\n   timestamp: 2026-09-19T15:21:14.742051+00:00\n+- actor: claude-code\n+  id: 01m2x43zxk53qjgq1f7k9yxs5y\n+  text: |-\n+    ### implement — changed\n+    - evidence: 5 files — Sources/FoundationModelsExtras/DotfolderStacking.swift (new), Sources/FoundationModelsExtras/PathConfinement.swift (new), Sources/FoundationModelsExtras/DotfolderStack.swift, Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift (new), Tests/FoundationModelsExtrasTests/DotfolderStackTests.swift; `swift build --build-tests` 0 warnings; `swift test` 530 tests, 0 failures\n+    - next: `/review` on ^qpfymzm; the Skills-side deletion of `PathConfinement` is task ^zc95get\n+  timestamp: 2026-09-19T15:21:22.611024+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -39,13 +39,6 @@\n\n     Verification: `swift build --build-tests` gives 0 warnings; `swift test` gives 530 tests in 5 bundles, 0 failures.\n   timestamp: 2026-09-19T15:21:14.742051+00:00\n-- actor: claude-code\n-  id: 01m2x43zxk53qjgq1f7k9yxs5y\n-  text: |-\n-    ### implement — changed\n-    - evidence: 5 files — Sources/FoundationModelsExtras/DotfolderStacking.swift (new), Sources/FoundationModelsExtras/PathConfinement.swift (new), Sources/FoundationModelsExtras/DotfolderStack.swift, Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift (new), Tests/FoundationModelsExtrasTests/DotfolderStackTests.swift; `swift build --build-tests` 0 warnings; `swift test` 530 tests, 0 failures\n-    - next: `/review` on ^qpfymzm; the Skills-side deletion of `PathConfinement` is task ^zc95get\n-  timestamp: 2026-09-19T15:21:22.611024+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","transaction_id":null}
{"id":"01M2X478TED01YCMK8N29Q4RS2","timestamp":"2026-09-19T15:23:10.030480Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -46,6 +46,13 @@\n     - evidence: 5 files — Sources/FoundationModelsExtras/DotfolderStacking.swift (new), Sources/FoundationModelsExtras/PathConfinement.swift (new), Sources/FoundationModelsExtras/DotfolderStack.swift, Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift (new), Tests/FoundationModelsExtrasTests/DotfolderStackTests.swift; `swift build --build-tests` 0 warnings; `swift test` 530 tests, 0 failures\n     - next: `/review` on ^qpfymzm; the Skills-side deletion of `PathConfinement` is task ^zc95get\n   timestamp: 2026-09-19T15:21:22.611024+00:00\n+- actor: claude-code\n+  id: 01m2x478te8zprnmmzmb1e1zg2\n+  text: |-\n+    ### test — green\n+    - evidence: swift build --build-tests — 0 warnings, build complete; swift test — 301 tests, 0 failed, 0 skipped\n+    - next: ready for review\n+  timestamp: 2026-09-19T15:23:10.030110+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -46,13 +46,6 @@\n     - evidence: 5 files — Sources/FoundationModelsExtras/DotfolderStacking.swift (new), Sources/FoundationModelsExtras/PathConfinement.swift (new), Sources/FoundationModelsExtras/DotfolderStack.swift, Tests/FoundationModelsExtrasTests/DotfolderStackingTests.swift (new), Tests/FoundationModelsExtrasTests/DotfolderStackTests.swift; `swift build --build-tests` 0 warnings; `swift test` 530 tests, 0 failures\n     - next: `/review` on ^qpfymzm; the Skills-side deletion of `PathConfinement` is task ^zc95get\n   timestamp: 2026-09-19T15:21:22.611024+00:00\n-- actor: claude-code\n-  id: 01m2x478te8zprnmmzmb1e1zg2\n-  text: |-\n-    ### test — green\n-    - evidence: swift build --build-tests — 0 warnings, build complete; swift test — 301 tests, 0 failed, 0 skipped\n-    - next: ready for review\n-  timestamp: 2026-09-19T15:23:10.030110+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","transaction_id":null}
{"id":"01M2X48CB0CQ0GC50HQQE9ZDA0","timestamp":"2026-09-19T15:23:46.400941Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -53,6 +53,13 @@\n     - evidence: swift build --build-tests — 0 warnings, build complete; swift test — 301 tests, 0 failed, 0 skipped\n     - next: ready for review\n   timestamp: 2026-09-19T15:23:10.030110+00:00\n+- actor: claude-code\n+  id: 01m2x48cazskwesr3bb47pgvjw\n+  text: |-\n+    ### commit — changed\n+    - evidence: bd5c277 feat(dotfolder-stack): add a generic DotfolderStacking interface\n+    - next: send the task for review.\n+  timestamp: 2026-09-19T15:23:46.399859+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","reverse_patch":"--- original\n+++ modified\n@@ -53,13 +53,6 @@\n     - evidence: swift build --build-tests — 0 warnings, build complete; swift test — 301 tests, 0 failed, 0 skipped\n     - next: ready for review\n   timestamp: 2026-09-19T15:23:10.030110+00:00\n-- actor: claude-code\n-  id: 01m2x48cazskwesr3bb47pgvjw\n-  text: |-\n-    ### commit — changed\n-    - evidence: bd5c277 feat(dotfolder-stack): add a generic DotfolderStacking interface\n-    - next: send the task for review.\n-  timestamp: 2026-09-19T15:23:46.399859+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: doing\n","transaction_id":null}
{"id":"01M2X495RY5M3RKYV4B965BFDC","timestamp":"2026-09-19T15:24:12.446057Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -62,7 +62,7 @@\n   timestamp: 2026-09-19T15:23:46.399859+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Make DotfolderStacking a generic interface, and the only file access\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -62,7 +62,7 @@\n   timestamp: 2026-09-19T15:23:46.399859+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Make DotfolderStacking a generic interface, and the only file access\n ---\n","transaction_id":null}
{"id":"01M2X4E1RM9KRAGGX42FKMCMJ2","timestamp":"2026-09-19T15:26:52.180294Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -62,8 +62,8 @@\n   timestamp: 2026-09-19T15:23:46.399859+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: a480\n title: Make DotfolderStacking a generic interface, and the only file access\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -62,8 +62,8 @@\n   timestamp: 2026-09-19T15:23:46.399859+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n-position_column: done\n-position_ordinal: a480\n+position_column: review\n+position_ordinal: '80'\n title: Make DotfolderStacking a generic interface, and the only file access\n ---\n ## What\n","transaction_id":null}
{"id":"01M2X4E3AZ9WVKJV9X5T2DXMAQ","timestamp":"2026-09-19T15:26:53.791978Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -60,6 +60,13 @@\n     - evidence: bd5c277 feat(dotfolder-stack): add a generic DotfolderStacking interface\n     - next: send the task for review.\n   timestamp: 2026-09-19T15:23:46.399859+00:00\n+- actor: claude-code\n+  id: 01m2x4e3az9ngst36hs9endx3x\n+  text: |-\n+    ### review — clean\n+    - evidence: 0 findings — `review sha HEAD~1..HEAD` (bd5c277), 5 files reviewed, 7 validator runs attempted, 0 failed, 0 refuted\n+    - next: task moved to done\n+  timestamp: 2026-09-19T15:26:53.791558+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: done\n","reverse_patch":"--- original\n+++ modified\n@@ -60,13 +60,6 @@\n     - evidence: bd5c277 feat(dotfolder-stack): add a generic DotfolderStacking interface\n     - next: send the task for review.\n   timestamp: 2026-09-19T15:23:46.399859+00:00\n-- actor: claude-code\n-  id: 01m2x4e3az9ngst36hs9endx3x\n-  text: |-\n-    ### review — clean\n-    - evidence: 0 findings — `review sha HEAD~1..HEAD` (bd5c277), 5 files reviewed, 7 validator runs attempted, 0 failed, 0 refuted\n-    - next: task moved to done\n-  timestamp: 2026-09-19T15:26:53.791558+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: done\n","transaction_id":null}
{"id":"01M2X4EMC441XQJQ2V9YMQ2Z1W","timestamp":"2026-09-19T15:27:11.236459Z","op":"update","item_id":"01M2X30BQKPD2MKA5EJQPFYMZM","forward_patch":"--- original\n+++ modified\n@@ -67,6 +67,15 @@\n     - evidence: 0 findings — `review sha HEAD~1..HEAD` (bd5c277), 5 files reviewed, 7 validator runs attempted, 0 failed, 0 refuted\n     - next: task moved to done\n   timestamp: 2026-09-19T15:26:53.791558+00:00\n+- actor: claude-code\n+  id: 01m2x4emc3g3mryk69mwhcv1b9\n+  text: |-\n+    ### finish iteration 1 — clean\n+    - implement: changed — 5 files\n+    - test: green — swift test, 301 passed, 0 failed, 0 skipped\n+    - commit: bd5c277\n+    - review: clean — 0 findings, task moved to done\n+  timestamp: 2026-09-19T15:27:11.235979+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: done\n","reverse_patch":"--- original\n+++ modified\n@@ -67,15 +67,6 @@\n     - evidence: 0 findings — `review sha HEAD~1..HEAD` (bd5c277), 5 files reviewed, 7 validator runs attempted, 0 failed, 0 refuted\n     - next: task moved to done\n   timestamp: 2026-09-19T15:26:53.791558+00:00\n-- actor: claude-code\n-  id: 01m2x4emc3g3mryk69mwhcv1b9\n-  text: |-\n-    ### finish iteration 1 — clean\n-    - implement: changed — 5 files\n-    - test: green — swift test, 301 passed, 0 failed, 0 skipped\n-    - commit: bd5c277\n-    - review: clean — 0 findings, task moved to done\n-  timestamp: 2026-09-19T15:27:11.235979+00:00\n depends_on:\n - 01M2X2EMGFB08X9F0XV40N54DH\n position_column: done\n","transaction_id":null}
