# This corpus deliberately mixes LF and CRLF line endings to exercise
# IgnoreProcessor's CRLF handling (see IgnoreGitParityTests.swift). Without
# this, the repo's core.autocrlf=input normalizes every CRLF to LF on
# `git add`, silently deleting the very thing the fixture is testing.
gitignore.txt -text
