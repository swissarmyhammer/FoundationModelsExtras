/// The `FoundationModelsExtras` module's placeholder root.
///
/// This is scaffolding only: the substrate leaf at the bottom of the
/// swissarmyhammer FoundationModels family dependency graph (see plan.md).
/// It will be replaced by the three pillars — the `SlashCommand` /
/// `SlashCommandProviding` vocabulary (plan.md §2), `DotfolderStack`
/// (plan.md §3), and the Stencil-backed `TemplateEngine` (plan.md §4) — as
/// each lands in its own subsequent task.
public enum FoundationModelsExtras {}
